<!--create-profile.wxml-->
<view class="container">
  <!-- 步骤指示器 -->
  <view class="step-indicator">
    <view 
      class="step-item {{currentStep >= 1 ? 'active' : ''}}" 
    >
      <view class="step-icon">✏️</view>
      <text class="step-title">档案信息</text>
    </view>
    <view 
      class="step-item {{currentStep >= 2 ? 'active' : ''}}" 
    >
      <view class="step-icon">📷</view>
      <text class="step-title">拍照检测</text>
    </view>
    <view 
      class="step-item {{currentStep >= 3 ? 'active' : ''}}" 
    >
      <view class="step-icon">✅</view>
      <text class="step-title">完成</text>
    </view>
  </view>

  <!-- 主要内容区域 -->
  <view class="content-area">
    <!-- 第一步：档案信息 -->
    <view class="step-content" wx:if="{{currentStep === 1}}">
      <view class="section-title">档案信息</view>
      
      <!-- 用户选择区域 -->
      <view class="user-selection-area">
        <!-- 加载状态 -->
        <view class="loading-state" wx:if="{{loading}}">
          <text class="loading-text">加载中...</text>
        </view>
        
        <!-- 错误状态 -->
        <view class="error-state" wx:elif="{{error}}">
          <text class="error-text">{{errorMessage}}</text>
          <button class="retry-btn" bindtap="onRetry">重试</button>
        </view>
        
        <!-- 正常状态 -->
        <view class="selection-placeholder" wx:else bindtap="showUserSelector">
          <text class="placeholder-text">{{selectedUser ? selectedUser.nickname : '请选择用户'}}</text>
        </view>
      </view>
    </view>

    <!-- 未登录用户提示 -->
    <view class="step-content" wx:if="{{currentStep === 1 && !selectedUser && !loading}}">
      <view class="section-title">欢迎使用健康检测</view>
      <view class="welcome-message">
        <text class="welcome-text">您可以直接进行拍照检测，无需完善个人信息</text>
        <text class="welcome-desc">点击下方按钮开始检测</text>
      </view>
    </view>

    <!-- 第二步：选择档案 -->
    <view class="step-content" wx:if="{{currentStep === 2}}">
      <view class="section-title">请选择档案</view>
      
      <!-- 档案列表 -->
      <view class="profile-list" wx:if="{{selectedUserProfiles.length > 0}}">
        <view 
          class="profile-item" 
          wx:for="{{selectedUserProfiles}}" 
          wx:key="id"
          bindtap="selectProfile"
          data-profile="{{item}}"
          data-index="{{index}}"
        >
          <view class="profile-info">
            <view class="profile-name">{{item.name}}</view>
            <view class="profile-meta">
              <text class="meta-item">检测次数: {{item.photoCount}}次</text>
            </view>
          </view>
          <view class="profile-arrow">></view>
        </view>
      </view>

      <!-- 空状态 -->
      <view class="empty-state" wx:if="{{selectedUserProfiles.length === 0}}">
        <text class="empty-text">暂无档案，请新增档案</text>
      </view>

      <!-- 新增档案 -->
      <view class="add-profile-section">
        <view class="add-profile-btn" bindtap="addNewProfile">
          <text class="add-icon">➕</text>
          <text class="add-text">新增档案</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 操作按钮 -->
  <view class="action-buttons">
    <button 
      class="btn btn-prev" 
      wx:if="{{currentStep > 1}}"
      bindtap="prevStep"
    >上一步</button>
    <button 
      class="btn btn-next" 
      wx:if="{{currentStep === 1 && selectedUser}}"
      bindtap="nextStep"
    >下一步</button>
    <button 
      class="btn btn-primary" 
      wx:if="{{currentStep === 1 && !selectedUser && !loading}}"
      bindtap="startPhotoDetection"
    >开始拍照检测</button>
  </view>

  <!-- 用户选择弹窗 -->
  <t-popup
    visible="{{showUserSelector}}"
    placement="bottom"
    bind:close="closeUserSelector"
  >
    <view class="user-selector-content">
      <view class="selector-header">
        <text class="selector-cancel" bindtap="closeUserSelector">取消</text>
        <text class="selector-title">请选择</text>
        <text class="selector-confirm" bindtap="confirmUserSelection">确定</text>
      </view>
      
      <scroll-view class="selector-options" scroll-y="true">
        <view 
          class="selector-option {{selectedUser && selectedUser.id === item.id ? 'selected' : ''}} {{currentSubUser && currentSubUser.id === item.id ? 'default-user' : ''}}" 
          wx:for="{{subUsers}}" 
          wx:key="id"
          bindtap="selectUser"
          data-user="{{item}}"
        >
          <text class="option-text">
            {{item.nickname}}
          </text>
          <text class="option-check" wx:if="{{selectedUser && selectedUser.id === item.id}}">✅</text>
        </view>
        <view class="selector-option add-option" bindtap="addNewUser">
          <text class="option-text">新增用户</text>
        </view>
      </scroll-view>
    </view>
  </t-popup>

  <!-- 简单的自定义弹窗 -->
  <view class="custom-popup" wx:if="{{showUserSelector}}">
    <view class="custom-overlay" bindtap="closeUserSelector"></view>
    <view class="custom-popup-content">
      <view class="selector-header">
        <text class="selector-cancel" bindtap="closeUserSelector">取消</text>
        <text class="selector-title">请选择</text>
        <text class="selector-confirm" bindtap="confirmUserSelection">确定</text>
      </view>
      
      <scroll-view class="selector-options" scroll-y="true">
        <view 
          class="selector-option {{selectedUser && selectedUser.id === item.id ? 'selected' : ''}} {{currentSubUser && currentSubUser.id === item.id ? 'default-user' : ''}}" 
          wx:for="{{subUsers}}" 
          wx:key="id"
          bindtap="selectUser"
          data-user="{{item}}"
        >
          <text class="option-text"> {{item.nickname}} </text>
          <text class="option-check" wx:if="{{selectedUser && selectedUser.id === item.id}}">✅</text>
        </view>
        <view class="selector-option add-option" bindtap="addNewUser">
          <text class="option-text">新增用户</text>
        </view>
      </scroll-view>
    </view>
  </view>

  <!-- 用户信息输入弹窗 - 第一步：昵称 -->
  <t-popup
    visible="{{showNicknamePopup}}"
    placement="bottom"
    bind:close="closeNicknamePopup"
  >
    <view class="popup-content">
      <view class="popup-header">
        <text class="popup-title">{{popupTitle}}</text>
        <text class="popup-close" bindtap="closeNicknamePopup">✕</text>
      </view>
      
      <view class="form-content">
        <view class="form-item">
          <text class="form-label">用户昵称</text>
          <input 
            class="form-input" 
            placeholder="请输入用户昵称" 
            value="{{userForm.nickname}}"
            bindinput="onNicknameInput"
            maxlength="20"
          />
        </view>
        <view class="form-item">
          <text class="form-label">性别</text>
          <view class="gender-selector">
            <view 
              class="gender-option {{userForm.gender === '1' ? 'active' : ''}}" 
              bindtap="selectGender" 
              data-gender="1"
            >
              <text class="gender-icon">👨</text>
              <text class="gender-text">男</text>
            </view>
            <view 
              class="gender-option {{userForm.gender === '2' ? 'active' : ''}}" 
              bindtap="selectGender" 
              data-gender="2"
            >
              <text class="gender-icon">👩</text>
              <text class="gender-text">女</text>
            </view>
          </view>
        </view>
      </view>
      
      <view class="popup-actions">
        <button class="popup-btn cancel-btn" bindtap="closeNicknamePopup">取消</button>
        <button class="popup-btn confirm-btn" bindtap="nextUserStep">下一步</button>
      </view>
    </view>
  </t-popup>

  <!-- 用户信息输入弹窗 - 第二步：出生年份 -->
  <t-popup
    visible="{{showBirthYearPopup}}"
    placement="bottom"
    bind:close="closeBirthYearPopup"
  >
    <view class="popup-content">
      <view class="popup-header">
        <text class="popup-title">{{popupTitle}}</text>
        <text class="popup-close" bindtap="closeBirthYearPopup">✕</text>
      </view>
      
      <view class="form-content">
        <view class="form-item">
          <text class="form-label">出生年份</text>
          <view class="selection-placeholder" bindtap="showBirthYearSelector">
            <text class="placeholder-text">{{userForm.birthYear || '请选择出生年份'}}</text>
          </view>
        </view>
      </view>
      
      <view class="popup-actions">
        <button class="popup-btn cancel-btn" bindtap="prevUserStep">上一步</button>
        <button class="popup-btn confirm-btn" bindtap="nextUserStep">下一步</button>
      </view>
    </view>
  </t-popup>

  <!-- 用户信息输入弹窗 - 第二步：地址 -->
  <t-popup
    visible="{{showAddressPopup}}"
    placement="bottom"
    bind:close="closeAddressPopup"
  >
    <view class="popup-content">
      <view class="popup-header">
        <text class="popup-title">{{popupTitle}}</text>
        <text class="popup-close" bindtap="closeAddressPopup">✕</text>
      </view>
      
      <view class="form-content">
        <view class="form-item">
          <text class="form-label">省份</text>
          <view class="selection-placeholder" bindtap="showProvinceSelector">
            <text class="placeholder-text">{{userForm.province || '请选择省份'}}</text>

          </view>
        </view>
        <view class="form-item">
          <text class="form-label">城市</text>
          <view class="selection-placeholder" bindtap="showCitySelector">
            <text class="placeholder-text">{{userForm.city || '请选择城市'}}</text>
          </view>
        </view>
        <view class="form-item">
          <text class="form-label">区县</text>
          <view class="selection-placeholder" bindtap="showDistrictSelector">
            <text class="placeholder-text">{{userForm.district || '请选择区县'}}</text>
          </view>
        </view>
      </view>
      
      <view class="popup-actions">
        <button class="popup-btn cancel-btn" bindtap="prevUserStep">上一步</button>
        <button class="popup-btn confirm-btn" bindtap="saveUserInfo">{{isCompleteMode ? '保存' : '确定'}}</button>
      </view>
    </view>
  </t-popup>

  <!-- 出生年份选择弹窗 -->
  <t-popup
    visible="{{showBirthYearSelector}}"
    placement="bottom"
    bind:close="closeBirthYearSelector"
  >
    <view class="user-selector-content">
      <view class="selector-header">
        <text class="selector-cancel" bindtap="closeBirthYearSelector">取消</text>
        <text class="selector-title">请选择出生年份</text>
        <text class="selector-confirm" bindtap="confirmBirthYear">确定</text>
      </view>
      
      <scroll-view class="selector-options" scroll-y="true">
        <view 
          class="selector-option" 
          wx:for="{{birthYearOptions}}" 
          wx:key="*this"
          bindtap="selectBirthYear"
          data-year="{{item}}"
        >
          <text class="option-text">{{item}}年</text>
        </view>
      </scroll-view>
    </view>
  </t-popup>

  <!-- 省份选择弹窗 -->
  <t-popup
    visible="{{showProvinceSelector}}"
    placement="bottom"
    bind:close="closeProvinceSelector"
  >
    <view class="user-selector-content">
      <view class="selector-header">
        <text class="selector-cancel" bindtap="closeProvinceSelector">取消</text>
        <text class="selector-title">请选择省份</text>
        <text class="selector-confirm" bindtap="confirmProvince">确定</text>
      </view>
      
      <scroll-view class="selector-options" scroll-y="true">
        <view 
          class="selector-option" 
          wx:for="{{provinceOptions}}" 
          wx:key="*this"
          bindtap="selectProvince"
          data-province="{{item}}"
        >
          <text class="option-text">{{item}}</text>
        </view>
      </scroll-view>
    </view>
  </t-popup>

  <!-- 城市选择弹窗 -->
  <t-popup
    visible="{{showCitySelector}}"
    placement="bottom"
    bind:close="closeCitySelector"
  >
    <view class="user-selector-content">
      <view class="selector-header">
        <text class="selector-cancel" bindtap="closeCitySelector">取消</text>
        <text class="selector-title">请选择城市</text>
        <text class="selector-confirm" bindtap="confirmCity">确定</text>
      </view>
      
      <scroll-view class="selector-options" scroll-y="true">
        <view 
          class="selector-option" 
          wx:for="{{cityOptions}}" 
          wx:key="*this"
          bindtap="selectCity"
          data-city="{{item}}"
        >
          <text class="option-text">{{item}}</text>
        </view>
      </scroll-view>
    </view>
  </t-popup>

  <!-- 区县选择弹窗 -->
  <t-popup
    visible="{{showDistrictSelector}}"
    placement="bottom"
    bind:close="closeDistrictSelector"
  >
    <view class="user-selector-content">
      <view class="selector-header">
        <text class="selector-cancel" bindtap="closeDistrictSelector">取消</text>
        <text class="selector-title">请选择区县</text>
        <text class="selector-confirm" bindtap="confirmDistrict">确定</text>
      </view>
      
      <scroll-view class="selector-options" scroll-y="true">
        <view 
          class="selector-option" 
          wx:for="{{districtOptions}}" 
          wx:key="*this"
          bindtap="selectDistrict"
          data-district="{{item}}"
        >
          <text class="option-text">{{item}}</text>
        </view>
      </scroll-view>
    </view>
  </t-popup>

  <!-- 身体部位选择弹窗 -->
  <t-popup
    visible="{{showBodyPartPopup}}"
    placement="bottom"
    bind:close="closeBodyPartPopup"
  >
    <view class="popup-content">
      <view class="popup-header">
        <text class="popup-title">选择身体部位</text>
        <text class="popup-close" bindtap="closeBodyPartPopup">✕</text>
      </view>
      
      <view class="body-part-grid">
        <view 
          class="body-part-item" 
          wx:for="{{bodyParts}}" 
          wx:key="value"
          bindtap="selectBodyPart"
          data-part="{{item}}"
        >
          <text class="part-icon">{{item.icon}}</text>
          <text class="part-name">{{item.name}}</text>
        </view>
      </view>
    </view>
  </t-popup>

  <!-- 详细部位选择弹窗 -->
  <t-popup
    visible="{{showDetailPartPopup}}"
    placement="bottom"
    bind:close="closeDetailPartPopup"
  >
    <view class="popup-content">
      <view class="popup-header">
        <text class="popup-title">选择{{selectedBodyPart.name}}</text>
        <text class="popup-close" bindtap="closeDetailPartPopup">✕</text>
      </view>
      
      <!-- 手指选择 -->
      <view class="detail-part-container" wx:if="{{selectedBodyPart.value === 'leftHand' || selectedBodyPart.value === 'rightHand'}}">
        <view class="hand-image-container">
          <view class="hand-emoji {{selectedBodyPart.value === 'leftHand' ? 'left-hand' : 'right-hand'}}">
            {{selectedBodyPart.value === 'leftHand' ? '🤚' : '🤚'}}
          </view>
          <!-- 手指点击区域 -->
          <view class="finger-areas">
            <view 
              class="finger-area {{selectedBodyPart.value === 'leftHand' ? 'left-thumb-area' : 'right-thumb-area'}}" 
              bindtap="selectDetailPart" 
              data-part="{{detailParts[0]}}"
              data-index="0"
            ></view>
            <view 
              class="finger-area {{selectedBodyPart.value === 'leftHand' ? 'left-index-area' : 'right-index-area'}}" 
              bindtap="selectDetailPart" 
              data-part="{{detailParts[1]}}"
              data-index="1"
            ></view>
            <view 
              class="finger-area {{selectedBodyPart.value === 'leftHand' ? 'left-middle-area' : 'right-middle-area'}}" 
              bindtap="selectDetailPart" 
              data-part="{{detailParts[2]}}"
              data-index="2"
            ></view>
            <view 
              class="finger-area {{selectedBodyPart.value === 'leftHand' ? 'left-ring-area' : 'right-ring-area'}}" 
              bindtap="selectDetailPart" 
              data-part="{{detailParts[3]}}"
              data-index="3"
            ></view>
            <view 
              class="finger-area {{selectedBodyPart.value === 'leftHand' ? 'left-little-area' : 'right-little-area'}}" 
              bindtap="selectDetailPart" 
              data-part="{{detailParts[4]}}"
              data-index="4"
            ></view>
          </view>
        </view>
        <!-- 右手手指标签 -->
        <view class="finger-labels" wx:if="{{selectedBodyPart.value === 'rightHand'}}">
          <text 
            class="finger-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[0]}}"
            data-index="0"
          >大拇指</text>
          <text 
            class="finger-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[1]}}"
            data-index="1"
          >食指</text>
          <text 
            class="finger-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[2]}}"
            data-index="2"
          >中指</text>
          <text 
            class="finger-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[3]}}"
            data-index="3"
          >无名指</text>
          <text 
            class="finger-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[4]}}"
            data-index="4"
          >小指</text>
        </view>
        
        <!-- 左手手指标签 -->
        <view class="finger-labels" wx:if="{{selectedBodyPart.value === 'leftHand'}}">
          <text 
            class="finger-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[4]}}"
            data-index="4"
          >小指</text>
          <text 
            class="finger-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[3]}}"
            data-index="3"
          >无名指</text>
          <text 
            class="finger-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[2]}}"
            data-index="2"
          >中指</text>
          <text 
            class="finger-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[1]}}"
            data-index="1"
          >食指</text>
          <text 
            class="finger-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[0]}}"
            data-index="0"
          >大拇指</text>
        </view>
      </view>
      
      <!-- 脚趾选择 -->
      <view class="detail-part-container" wx:if="{{selectedBodyPart.value === 'leftFoot' || selectedBodyPart.value === 'rightFoot'}}">
        <view class="foot-image-container">
          <view class="foot-emoji {{selectedBodyPart.value === 'leftFoot' ? 'left-foot' : 'right-foot'}}">
            {{selectedBodyPart.value === 'leftFoot' ? '🦶' : '🦶'}}
          </view>
          <!-- 脚趾点击区域 -->
          <view class="toe-areas">
            <view 
              class="toe-area {{selectedBodyPart.value === 'leftFoot' ? 'left-big-toe-area' : 'right-big-toe-area'}}" 
              bindtap="selectDetailPart" 
              data-part="{{detailParts[0]}}"
              data-index="0"
            ></view>
            <view 
              class="toe-area {{selectedBodyPart.value === 'leftFoot' ? 'left-second-toe-area' : 'right-second-toe-area'}}" 
              bindtap="selectDetailPart" 
              data-part="{{detailParts[1]}}"
              data-index="1"
            ></view>
            <view 
              class="toe-area {{selectedBodyPart.value === 'leftFoot' ? 'left-third-toe-area' : 'right-third-toe-area'}}" 
              bindtap="selectDetailPart" 
              data-part="{{detailParts[2]}}"
              data-index="2"
            ></view>
            <view 
              class="toe-area {{selectedBodyPart.value === 'leftFoot' ? 'left-fourth-toe-area' : 'right-fourth-toe-area'}}" 
              bindtap="selectDetailPart" 
              data-part="{{detailParts[3]}}"
              data-index="3"
            ></view>
            <view 
              class="toe-area {{selectedBodyPart.value === 'leftFoot' ? 'left-little-toe-area' : 'right-little-toe-area'}}" 
              bindtap="selectDetailPart" 
              data-part="{{detailParts[4]}}"
              data-index="4"
            ></view>
          </view>
        </view>
        <!-- 右脚脚趾标签 -->
        <view class="toe-labels" wx:if="{{selectedBodyPart.value === 'rightFoot'}}">
          <text 
            class="toe-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[0]}}"
            data-index="0"
          >大脚趾</text>
          <text 
            class="toe-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[1]}}"
            data-index="1"
          >第二脚趾</text>
          <text 
            class="toe-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[2]}}"
            data-index="2"
          >第三脚趾</text>
          <text 
            class="toe-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[3]}}"
            data-index="3"
          >第四脚趾</text>
          <text 
            class="toe-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[4]}}"
            data-index="4"
          >小脚趾</text>
        </view>
        
        <!-- 左脚脚趾标签 -->
        <view class="toe-labels" wx:if="{{selectedBodyPart.value === 'leftFoot'}}">
          <text 
            class="toe-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[4]}}"
            data-index="4"
          >小脚趾</text>
          <text 
            class="toe-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[3]}}"
            data-index="3"
          >第四脚趾</text>
          <text 
            class="toe-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[2]}}"
            data-index="2"
          >第三脚趾</text>
          <text 
            class="toe-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[1]}}"
            data-index="1"
          >第二脚趾</text>
          <text 
            class="toe-label" 
            bindtap="selectDetailPart" 
            data-part="{{detailParts[0]}}"
            data-index="0"
          >大脚趾</text>
        </view>
      </view>
    </view>
  </t-popup>
</view> 